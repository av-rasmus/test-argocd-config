ingress-nginx:
  controller:
    admissionWebhooks:
      enabled: false
    publishService:
      enabled: true
    ingressClassResource:
      default: true
    config:
      "use-proxy-protocol": "false"
